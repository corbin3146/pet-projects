package asteroids;

public class Main {
	public static void main(String[] args) {
		//generate objects
		/* game loop structure
		 * move things
		 * calculate distances, que intersections to check
		 * check intersections
		 * add/remove objects
		 * loop
		 */
		//program structure:
		//graphics engine is given structure of positions, and new animations to play
		//objects will need to store position, direction, speed(could be global), size(index)
		//
		//problems:
		//what structure to store asteroid objects linked list?
		//
		//notes:
		//cool
	}
}
